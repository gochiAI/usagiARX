image:
  file: .gitpod.Dockerfile
tasks:
  - name: config
    command: flutter config --no-enable-android --no-enable-ios --no-enable-windows-desktop --enable-web
  - name: pub get
    command: flutter pub get
vscode:
  extensions:
    - dart-code.dart-code
    - dart-code.flutter
